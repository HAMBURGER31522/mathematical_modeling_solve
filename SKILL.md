---
name: math-modeling-solve
description: 端到端求解数学建模竞赛题并产出可提交论文（华数杯、CUMCM 国赛、MCM/ICM 美赛、MathorCup、华为杯、电工杯等通用）。覆盖读题、数据体检、口径审定、选模、编码求解、统计证书、深度审查、图表、LaTeX 论文、编译修错、赛制合规打包。当用户给出建模竞赛题目要求完整求解或写竞赛论文，或提到数学建模、建模竞赛、赛题、数模论文时使用。
---

# math-modeling-solve：数学建模竞赛端到端求解

## 元规则（任何阶段不得违反）

1. **先出数，再写文**：论文中任何数值必须先由代码算出并落盘；禁止先写空论文再补数，禁止编造数字。
2. **结论必须带与其方法家族匹配的证书**（家族→证书映射见 references/algorithm-redlines.md R2）。点估计贴线不是证书。
3. **忠实题面口径**：判据、约束、边界规则按题面原文实现；模型中每条约束必须能指到题面原文，指不到即私货（R1 fail）。
4. **Gate 纪律**：每道 Gate 不通过不得进入下一阶段；每道 Gate 最多 2 轮修复、全程最多 8 轮，第 2 轮仍不过写 `结果/gates/G<n>-decision-memo.md`（问题、已尝试、残余风险、如实降级措辞），按降级协议继续，不得无限循环。**本条不适用于 R1/R2/R4/R6/R7**：不可豁免红线（R1/R2/R4/R6）未解除时，decision-memo 只能记录停工原因与已试方案，**不构成进入下一阶段的依据**；R7（赛制合规）未清零时**不得打包提交**，memo 同样不放行。迭代上限用完仍未解除，就砍问题范围或砍加分项来腾预算修红线，不许带着它往下走。
5. **references 是判定依据，不是选读**：P0 摘录合规条款前先读 compliance.md；进 P3 前必须读完 computation-standards.md；进 P4 前读完 algorithm-redlines.md 与 depth-review.md；进 P5/P6/P7 前分别读完 figures/paper-latex/compliance。未读而声称 Gate 通过 = 该 Gate 无效。
6. **端到端最小闭环优先**：P2 定型后先用 1% 规模把 P3→P7 全链路跑通一遍（粗数字 + 骨架论文 + 能编译的 PDF），确认交付形态存在再回头提升精度。任何时刻磁盘上都要有一份"可提交的最差版本"。
7. **Gate 台账**：每道 Gate 判定写入 `结果/gates/G<n>.md`，固定格式 `判定(PASS/FAIL) | 依据工件路径 | 关键数值 | 未决项`。只写 PASS 不带证据路径与数值 = 未判定。文件存在、字段齐全、数字一致等**结构性检查通过不构成科学正确性结论**，不得据此覆盖任何红线。

## 时间预算与降级协议

P0 写 `结果/预算.md`：总时限、阶段配额（建议 P0–P2 15%、P3 40%、P4 10%、P5 10%、P6 15%、P7 10%）、主计算机时上限。设三个冻结点：剩 30% 时禁止新增模型路线；剩 15% 时只修阻断项；剩 5% 时只允许编译、合规、打包和已冻结数字修复。

超预算的保命优先序（先砍最右）：**赛制合规 > 数字一致性 > 证书达标 > 论文完整 > 图表精美 > 深度加分项**。

- **D0 无数据附件**：产出 `结果/数据来源表.md`（题面给定/公开可引用/必须假设）；假设数据进口径表标 S4，正文不得把假设值写成测量值。跳过数据体检的读表部分，不跳过 G1。
- **D1 纯证明/无计算问**：该问 G3 改为证明检查单（假设、引理、结论、反例搜索），禁止为过 G3 编造仿真数；G5 证据链改为条件示意图→证明结构图→边界情形。
- **D2 时限压缩**（禁止静默砍采样，砍了必须写进 `结果/降级声明.md` 和摘要末句）：
  - 不可砍：G1 口径审定、R1/R2/R7、核心原语神谕单测、选择与认证样本分离。
  - 先砍：赏金表低比项、第三候选路线、灵敏度外的装饰图、全文第二轮深度审查（保留每问一轮）。
  - 再砍：问题个数——少问写透且证书过线，禁止各问全部点估计。
  - 计算侧：先换充分统计量/增量估计量，再减扫描网格点，最后才减认证样本量；认证不确定度一旦超 Δ/3，该问结论降级为"未分辨"，不得报满精度答案。

## 工作流（P0–P7）

复制此清单跟踪进度：

```
- [ ] P0 输入盘点 + 数据体检 + 预算（G0：体检表逐列有数字、题面↔数据逐条对照）
- [ ] P1 口径审定：决策表 + 语义验收测试清单（G1）
- [ ] P2 拆题与选模：主路线 + 独立核验路线 + 承重探针（G2）
- [ ] P3 实现与计算：共享内核 → 神谕单测 → 主计算 → 证书 → 合理性检查（G3）
- [ ] P4 深度审查：五件工件 + 外部核验，红线逐条判（G4）
- [ ] P5 图表：证据链图组 + 逐张质检（G5）
- [ ] P6 论文：分层篇幅 + 宏回填 + 三轮自审（G6）
- [ ] P7 编译 + 合规 + 复现闭环 + 打包（G7）
```

### P0 输入盘点与数据体检

- 通读题目 PDF（所有小问与图注）、数据附件、格式规范、AI 章程；摘录硬性条款到 `结果/合规摘录.md`——页数条款（硬上限还是建议值）、命名规则、AI 声明格式、**指定模板/编译链**（有官方模板时锁定 class 与引擎，禁止擅自替换或加宏包）。
- **数据体检（强制工件 `结果/数据体检.md`）**：每个附件逐列给规模、量纲、min/max/mean/std、缺失异常；然后①与题面声明逐项比对（尺寸/范围/数量在数据里是否成立）；②反推隐含结构（重复值、平移/对称配对、边界贴合率、聚类）；③登记一切数据与题面的矛盾点转入 P1。数据与题面冲突时以数据为准并在论文披露。
- 建目录：`求解/`（每问一子目录 + `求解/core/` 共享内核）、`论文/`、`结果/`、`图/`；列交付物清单；写 `结果/预算.md`。

**G0 通过标准（不是"文件齐"）**：`结果/数据体检.md` 每个附件逐列给出规模/量纲/min-max-mean-std/缺失异常的**数字**；与题面声明的比对逐条列出"题面条款 ↔ 实测值"两列；隐含结构反推至少给 1 项定量结论，或写明"已查重复值/平移对称/边界贴合率/聚类共 N 类，均未发现"；矛盾点清单为空时必须写明已查项。**只写"已比对，无矛盾""数据正常"而无数字 = fail**（实测 10/10 全员栽在"数据与题面矛盾未发现"，基线的制胜发现正来自这一步）。`结果/合规摘录.md` 与 `结果/预算.md` 同时落盘。

**事实／假设／推断三分清单（P0 产出，贯穿全程）**：把手上的每一条信息归入三类之一，写进 `notes/三分清单.md`：

| 类 | 定义 | 纪律 |
|---|---|---|
| **事实** | 题面原文、附件数据、赛制规范里**白纸黑字**写着的 | 可直接引用，但要标出处（页码/单元格） |
| **假设** | 题面没说、你为了建模**主动选定**的 | 必须进论文的模型假设节并附合理性依据；每条假设在检验章要有对应处理（夹逼／对照／声明理由） |
| **推断** | 由事实 + 假设**推出来**的中间结论 | 必须能追溯到它依赖的事实与假设；依赖项一变，推断全部作废重来 |

三类混淆是口径事故的源头——把推断当事实用，后面所有结论都建在流沙上。

### P1 口径审定（G1）

把题面每个**可解释点**列成口径决策表 `结果/口径决策表.md`，每点给一个稳定编号（A1、A2…），内核代码里实现该口径的分支用注释标注编号，单测与跨问一致性表按编号对账：

| 编号 | 可解释点 | 题面原文 locator | 采用口径 | 被否口径（含常见错误实现） | 定量对照 |
|---|---|---|---|---|---|

locator 写法（页码/图号/单元格）见 algorithm-redlines.md §locator；关键口径把原文摘录或裁剪图存到 `结果/证据/`。

必查点（通用）：边界处理（截断/回绕/丢弃）、实体独立性（截断片段是否独立参与）、几何/对象的精确定义、单位与尺度、约束严格含义（≥ 还是 >、点估计还是概率保证）、数据列的物理含义、**答案的精度/有效位/取整方向**。按家族追加（评价类查指标定义/权重含义/量纲处理等）。

- 被否口径必须做**定量对照**：同一小实验下两种口径的关键数值差及其与效应量 Δ 的比。当场算不了（尚无代码）就登记进 `结果/口径对照/待办.md`（拟做实验、预期效应量、判定阈值），P3 强制兑现。
- 同时产出 `结果/语义验收测试.md`：从题面原文导出可判 PASS/FAIL 的行为断言（等变性、守恒/对账、极限情形、跨问一致），期望值来自题面而非"采用口径"，P3 实现为黑盒测试。
- **清单完整性复盘**（防"不知道自己理解错了"）：填完表后另起一遍**只读题面、不看已填表**的重建——逐句扫题面的定义、边界、对象身份、随机生成规则、单位、严格不等式，重新列一次可解释点，再与决策表对表。新出现的点必须补进表；两次结论冲突的点标 `BLOCKED`，只能靠题面更强证据、官方勘误、量纲/守恒/极限归谬或双口径并报解除，不得由"我倾向于"结案。

**G1 通过标准**：每点有原文支撑；被否口径全部进入定量对照（当场算出或登记待办），**禁止以定性"影响评估/可忽略/长径比大"结案**——给不出数字 = 该点未审定 = fail；复盘重建已做且无遗留 `BLOCKED`；有翻转风险的分歧必须在论文披露并归谬。

### P2 拆题与选模（G2）

- 判定每问方法家族（M1 仿真 / M2 精确优化 / M3 启发式 / M4 统计 ML / M5 数值方程 / M6 图组合 / M7 评价类，定义见 depth-review.md）。**方法层**决策只用本 skill 的 M1–M7 工具箱与本节规则；**实现层**若需标准算法（求解器、预测、评价法、图算法等），优先经算法资源路由库选一个有维护、有许可、钉定 commit 的实现，禁止手搓教科书算法或抄无许可代码。路由库查找顺序：环境变量 `MATH_MODEL_ROUTER_DIR` → 从本 skill 根目录逐级向上找 `math-model-resource-router/`（含 `scripts/query_catalog.py`）→ `git clone https://github.com/HAMBURGER31522/Mathematical_Modeling_Algorithm.git`。找到即按其 SKILL.md 工作流查目录并把证据（查询命令、命中条目、许可与 commit）记入 `选型.md`；查询为空或属声明缺口 = 有效信息，按其替代建议处理。题目专属的物理/几何/仿真代码不强制走路由库，但要在 `选型.md` 写明"自研，路由库不适用"一行。三步都不可用时记 N/A 照常自研。
- 每问一条**主路线** + 至少一条**独立核验路线**（不同估计量/解析极限/暴力小实例/不同离散化；不必是完整第二模型，但必须能对主结论产出可比数字，禁止稻草人）。存在实质路线分歧时列出候选并写裁决理由。
- **证书等级预告**：该方法能产出什么证书（R2 映射表）；产不出的要么换方法，要么预告论文降级措辞。
- **承重假设探针**：探针必须打在承重假设上（近似判据 vs 精确判据的小样本对照、边界口径的最小构型验证），"脚本没崩"不算探针；探针失败不得开主计算。
- 小规模决策空间优先精确/枚举方法；具备单调/嵌套结构的问题优先增量/充分统计量估计（一次跑出全曲线），写"不适用"必须证明结构不存在。

**G2 通过标准**：`求解/qN/选型.md` 落盘（主路线、核验路线、证书预告、探针数字、**实现来源行**——路由库证据 / "自研，路由库不适用" / N/A，缺行 = fail）；**且机时可行性已实测**——见下条。

**机时可行性（G2 硬项，实测非估算）**：进 P3 前必须用**真实内核**在小规模上量出单次试验耗时，把命令与输出贴进 `结果/分辨率预算.md`，再按 O(复杂度) 外推到目标规模与目标样本量。**只写估计值 = G2 fail。** 外推总机时超配额时，必须在 P3 开工前完成提速（优先序见 computation-standards §4），而不是开跑后再补救——实测教训：逐对循环外推 67 小时、写进预算表的却是"~30 min"，机时在 P3 中段烧光，交付物为零。提速后要**重测并更新预算表**，用新数字过 G2。

### P3 实现与计算（G3）

**进生产批之前必须先过退化规模检验（R13），四行结果落盘 `结果/gates/G3-退化检验.md`**：
个体数=1 的导通率、个体数=0 的存在性结论、判据阈值→0 与→大的连边数单调性。
这张表成本近零，却能拦住统计证书拦不住的口径错误——
**曾有一份 50 页、92 测试全过、G0–G7 全绿的交付物，答案错了近 70 倍，
而 `n=1` 这一行只要跑过，30 秒就能发现**（详见 algorithm-redlines.md 的事故复盘）。

**生成侧必须独立对表（R1 的延伸）**：题面附件的可核事实（如逐组边数）通常只经过**判定层**，
不经过**生成层**。必须另设一条生成侧检验：用生成器产出与附件同规模的构型，
比较碎片数、边数、连通簇规模的分布是否与附件同量级。两层各自对表，缺一层就是裸奔。

细则见 computation-standards.md，骨架：

1. **共享内核**：核心原语与判据实现为单一模块 `求解/core/`，各问一律 import，禁止逐问复制或各写一版；内核变更后所有依赖问重跑。
2. **神谕独立单测**：expected 来自手算/解析/独立算法，禁止由被测函数自产；退化清单按原语类型选；P1 的语义验收测试实现为黑盒测试。全过才准跑主计算。
3. **两阶段执行**：先算后画，计算脚本落盘结果与统计量，绘图脚本只读结果文件。
4. **分辨率预算**：Δ 来源按优先级取——题面明确精度 > 待比较方案目标差的 1/3 > 决策变量最小单位，**禁止自拟宽松 Δ**；每个 Δ 都要写出「取自哪条优先级 + 具体计算式」，**只写数值 = 该结论未审定 = fail**（实测反例：无题面精度时拍一个"Δ=0.5%"，样本量随之缩水一个量级）。认证不确定度 u ≤ Δ/3；预算表含**实测**单次试验耗时（G2 已量）与预计总机时，超配额按降级阶梯处理。**样本量按把握度排，不按临界点排**：`certify.py --solve-n` 同时给临界 n（真值≈p̂ 时一次终验只有约 50% 概率过线）与 90% 把握所需 n（通常是前者的 3 倍左右），证书协议只允许一次封存一次终验，机时按后者排。
5. **口径对照实验兑现**：`结果/口径对照/待办.md` 清零；差异 ≳ Δ 的分歧进论文假设章归谬。
6. **证书计算**（调 `scripts/certify.py`，别手写公式）：证书计划先行（估计量、适用假设、置信水平、单双侧、同时比较项数 K 与 alpha 分配、样本承诺、失败后唯一动作）；选择与认证分离（独立种子），候选一经封存，终验失败只能判不可行或重新选择并用全新批次，禁止反复换种子重测。不达标处置顺序：先判"真不可行还是区间太宽"，区间太宽先提精度（高效估计量/方差缩减/加算力）在**原答案点**重判，只有高精度下仍不可行才把答案向保守方向移动并记录代价；**禁止用抬高答案替代提高精度**。
7. **合理性检查**（`结果/合理性检查.md`，进 ledger 前）：极限/退化情形、单调性与形状、量纲与量级（用 5 行粗估模型独立对照，差一个数量级必须停下查代码，不得写进论文）、跨问互恰、退化输出识别（全 100%/全 0/无区分度/**全 inf**/**全 0 距离**默认判实现 bug，定位到代码行才放行）、**派生统计量交叉核对**（结果文件里的计数/汇总类字段——边数、命中数、分量数——必须与其来源结构直接重算比对；实测反例：`n_edges=0` 写进结果文件而邻接矩阵里实有 5 条边）。
8. **关键答案双路互证**：每问最终答案由主路线 + 核验路线各算一次，差异 ≳ Δ 必须定位原因，禁止取平均或择优报喜。
9. **答案稳定性复核**：≥2 个独立主种子重跑终验，按题目精度取整后答案不变；变了说明预算不足，回第 4 条。
10. 固定种子、增量落盘（崩溃可续）、`结果/results_ledger.json`（schema 见 computation-standards §7；**唯一权威答案**：每个被问量有且只有一条 authoritative 记录）。

**G3 通过标准（同时满足，缺一 fail，禁止用"字段非空"代替）**：`结果/分辨率预算.md`、`结果/合理性检查.md` 与 `结果/近似标定.md`（若用了任何近似）落盘；单测与语义验收测试全过，且神谕独立性抽查通过（expected 与被测函数同源 = fail）；单测数 < 50 属覆盖不足；口径对照待办清零；每个关键数值有 source 记录；认证样本量 ≥ 预算表所需值且种子独立于选择样本；合理性检查无未解释异常。**证书两级判定分开写，别焊成一个词**：

- **达标（R2，不可豁免）**：凡声称"满足约束/可行/达标"的条目，下界 ≥ 阈值。未达标的方案**不得进 ledger 与论文**——这是 fail，没有降级选项。
- **已分辨（R5，可降级）**：u ≤ Δ/3，其中 **u = p̂ 与单侧认证下界之差**。达标但未分辨的条目仍可入账，但必须写 `certificate.resolved=false`、把结论降到题面精度的下一档、在 `结果/降级声明.md` 记一行，**禁止报满精度答案**。R2 的 `pass` 与 R5 的三个字段（`certificate.delta/u/resolved`）一并记进 ledger，`certify.py --delta` 一次算全；不传 `--delta` 等于 R5 没判，权威答案整块不写 `certificate` 则 R2/R5 一起没判（`ledger.py --validate` 会 WARN）。
- **数字可回读（R6）**：每条 authoritative 必须写 `source.file` + `source.field`，且 `audit_numbers.py` 能从原始结果文件回读复算成功。回读不了 = 三向审计对该条退化成两向 = fail，不接受"人工核对过了"。

### P4 深度审查（G4，硬门禁）

对每问跑 depth-review.md 的五问流水线（审查深度分级见该文件），产出五件审查工件；另产**外部核验工件** `结果/审查/外部核验.md`（每问必产）：

1. **声明-实现一致性抽查表**：`| 论文口径原句 | 代码文件:行号 | 该行实际谓词 | 一致? |`，每问关键判据（连通/可行/接触/边界/目标函数）至少抽 3 处，一处不一致 = fail。
2. **跨问一致性表**：逐可解释点列各问实际采用口径（**从代码行号取证，不从论文取证**）+ 共用内核函数名；跨问不一致 = R1 红；确需分叉要有题面依据并回 G1 补双口径实验。
3. **量级/极限外部对照**（至少 2 项，写数字）：空/满/无约束等极限的解析界、量纲守恒、独立粗模型或理论公式量级；与对照差一个数量级且无书面调和 = fail。
4. **私货约束扫描**：每条模型约束指到题面原文，指不到 = R1 fail；端点/退化方案（只用一种资源、数量为 0、参数取边界）必须显式评估。

然后按 algorithm-redlines.md 逐条判定红线，每条写"判定 + 证据路径 + 关键数值"三元组。

**G4 通过标准**：R1、R2、R4、R6 必须全绿，任何一条红即回 P3 修复重跑，**不接受"已在论文披露"替代修复**；R3、R5 允许降级，但降级 = 删除或改写相关结论（全文措辞逐处降级 + 优缺点章写明实际强度等级 + 记入唯一台账 `结果/降级声明.md`，与 D2 砍项同表），**不得保留原答案原措辞**；R7 在 G7 判。禁止带着未解除的 FAIL 进入下一阶段。

### P5 图表（G5）

证据链四环（构型/机理 → 收敛或诊断 → 证书 → 灵敏度）**按适用性配置**：随机迭代类必须有收敛诊断；带约束认证的结论必须有证书图或证书表（按家族证书类型配，不硬套 MC 收敛曲线与置信下界图）；结论依赖关键参数才需要灵敏度图；不适用写 N/A 理由。装饰图不进正文。细则见 figures-standards.md。

**实现层处于 A/B 对比评估期**（查找顺序、工作流与对比协议见 figures-standards.md 首节）：figure-forge 库可用时，挑 2–3 张代表性正文图各画"手写"与"recipe"两版，写 `结果/作图AB对比.md` 给出逐项胜负与理由，其余图按顺手的路子画；两条路都遵守"图内数字必须引用计算变量"；figqa.py 独立审计照做不豁免。

先跑自动检查 `python scripts/figqa.py 图/ --tex 论文/main.tex --out 结果/figqa.json --contact 图/_contact.png`（空图/重复图/分辨率/未被引用），再逐张人工过重叠、误差带、截断轴、图注自足、缩放比。

**G5 通过标准**：**`figqa.py` 退出码必须为 0**——「生成的图未被正文引用」现在是 **FAIL 不是告警**（图做出来却没进论文＝论证链断了；要么正文引用并配解读段，要么移进补充图表附录并在正文指路，要么删除；骨架阶段可用 `--draft` 降级，成稿与门禁一律不得加该开关）。此外：`图/图表清单.md` 逐图落盘（文件名、证据链环节、被正文引用处、来源模板、质检结论 PASS/FAIL）；figure-forge 可用时 `结果/作图AB对比.md` 落盘（2–3 张双版对比 + 逐项理由），不可用时记 N/A 与原因；`结果/图面质检.md` 逐张写人工目检结论；`结果/figqa.json` 无未处置 FAIL；contact sheet `图/_contact.png` 已导出且目检记录在案；无与 ledger 数字矛盾的图。

### P6 论文（G6）

细则见 paper-latex.md，骨架：

- **篇幅底线（硬）**：四问全做的竞赛论文**正文低于 25 页基本等于没写透**（同题人工基线交付物 37 页）。不足 25 页不要交稿，回头补的是**内容不是水**：每问的检验与灵敏度、机理讨论、图表解读，以及分层附录（复现说明/核心代码/结果明细表/推导全过程/AI 声明）。注水同时丢 paper 与 rules 两维，比短更糟；章程另有硬性上限时以章程为准。
- **篇幅口径**：完整性优先的对象是**证据链**，不是页数。正文只承载证据链主线（模型设定、关键推导结论、主结果与证书、最关键的图、检验结论）；明细表、逐点扫描、推导全过程、次要对照、源码进附录。章程有建议页数时争取正文落在建议值内——这是 rules 维不失分的必要动作；删节判据是"删掉是否丢失证据链一环"，会丢的**移附录而不是删**；实在压不进时在 G7 台账记录为知情扣分项。
- **数字回填**：`python scripts/ledger.py --emit-tex 结果/results_ledger.json -o 论文/numbers.tex` 生成宏（含下界/样本量/阈值派生宏），关键数值正文只引用宏；宏注入失败时的人工核对是临时措施，审计报告必须逐条给论文页码 + ledger 键。无官方模板时用分节模板包 `assets/paper/`（主控 `main.tex` + 13 个分节 tex，见该目录 README）。
- **关键结论醒目集中（硬条件，2026-09-04 评委反馈）**：① 每问末尾放**结论块**（最终答案 + 证书 + 适用条件三行）；
  ② 全文放一张**结果汇总表**（一行一问：问题／模型类别／求解算法／最终答案／证书／所在章节）。
  评审意见明确指出"关键结论还可更醒目地集中呈现"，这是低成本高回报的一条。
- **摘要只写「算法 + 结果」**：每问一句 = 问题类型 + **加粗算法名** + **加粗结论与数字**；
  技术细节一律留正文（评审意见："部分技术细节略密"）。一句里粗体不超过两处。
- **独立模型检验章（硬条件）**：检验必须**独立成章**，不得散落在各问结尾。六个小节缺一不可——① 双路互证（两条独立实现算同一量，给一致性证据）② 与可核事实对表（题面/附件能验证的量）③ 参数灵敏度（逐参数扰动 → 结论是否翻转，给数字）④ 样本量与收敛（证书随样本量变化，说明为何够分辨相邻档）⑤ 稳健性与对照实验（换口径/换种子/换实现）⑥ 适用边界与失效域。对标同题人工基线：其检验章体量是全文第二大（仅次于最大的一问），我方历史交付物把它压成几句话，是 8 页 vs 53 页的最大单项缺口。
- **每问至少一张关键结果图进正文，且图后紧跟解读段**（"从图上看出什么 → 因此如何取舍"），不是只写"如图 X 所示"。
- **自审三轮**：① Claim-Evidence 映射（每个结论指到图/表/ledger 键/代码）；② 反过度主张扫描，词表 `最优|最低|证明|显著|必然|可忽略|严格|满足约束|可行|达标|推荐方案|最终答案|全局`，摘要与结论中每个数字必须 ⊆ ledger 中证书达标的 authoritative 条目，机会约束数字旁必须出现下界值；③ **终审强度校准**：对摘要 + 结论 + 各问答案重跑 depth-review 第一、二问，措辞强度不得高于任何一处实际等级，落盘 `结果/审查/终审强度校准.md`。
- **答案唯一性**：每问最终答案在摘要/正文/图表/结论中是同一个数；交叉验证值只出现在检验小节或附录并注明"用于互证，不作为答案"。

**G6 通过标准**：三向审计通过（`python scripts/audit_numbers.py --ledger 结果/results_ledger.json --numbers 论文/numbers.tex --tex 论文/main.tex --out 结果/审计报告.md` 无 FAIL，含单位/区间/样本量；论文引用 stale 键 = fail；脚本列出的未走宏数字逐条裁定完毕）；三轮自审记录落盘。

### P7 编译与合规打包（G7）

1. 按 `结果/合规摘录.md` 锁定的模板/引擎编译，优先 `latexmk -xelatex -halt-on-error -interaction=nonstopmode` 并**检查退出码**（无官方模板且中文写作才默认 xelatex+ctex；有 bib 走完整链 xelatex→biber/bibtex→xelatex×2）。用 `python scripts/latex_gate.py 论文/main.log --aux 论文/main.aux` 分类：**阻断项 10 类**＝真实错误（`^!`）、LaTeX Error、未定义命令、未定义引用、未定义引文、未定义汇总行、缺文件、缺字形、Float too large、Overfull（清单以 latex_gate.py 实现为准）；Underfull 仅处理严重且 PDF 目检可见项，残余 warning 记 `结果/编译白名单.md`。空图框在 log 里没有对应字符串，归 G5 的 figqa 与 PDF 目检负责。
2. 摘要页数按赛制要求时从 .aux 程序化核实（`latex_gate.py --abstract-label`）。
3. 合规检查单（compliance.md）逐项过：AI 声明、命名、匿名、首屏、附录。
   **清理清单（交付包内必须为零）**：`__pycache__/`、`*.pyc`、**`.pytest_cache/`**、`.aux/.log/.fls/.fdb_latexmk/.out/.synctex.gz`、临时渲染目录。（我方历史交付物是三家候选里唯一没清干净的——`.pytest_cache` 漏在清单外。）
4. **复现闭环**（在交付目录内）：必须落一个**可执行入口脚本**（`reproduce.py` 或 `reproduce.ps1`），而不是只在 README 里写命令——解包后不看论文也能跑起来。入口与 ledger 的 source.script 同源；用最小可核对子集再生 ledger 对应键且文件名逐一对应；论文引用的每个结果文件必须能被 run_all 写出，写不出 = fail。禁止"工作区能跑、交付包不能跑"。
5. manifest 逐文件（相对路径、SHA-256、生成命令）与包内比对；压缩后解压抽查。

**G7 通过标准**：编译阻断项清零；检查单全勾；复现闭环通过。

## 门禁通则（任何 Gate 都适用）

0. **每个 Gate 必须给出机器可检验的最小命令集与退出码**，落盘 `结果/gates/G<N>.md`：
   命令原文、退出码、判定。没有命令与退出码的 Gate 记录一律视为未执行（见第 1 条）。
   `--whitelist` 只对**已在白名单中逐条写明理由**的条目生效，不得用于整类抑制。

1. **禁止 vacuous pass**：检查项为空、被检对象不存在、脚本未实际执行 —— 一律记 **fail**，不得因"没有发现问题"而通过。
   门禁必须先证明自己**跑过了什么**，再给结论。
2. **门禁以脚本退出码为准**，不以人的复述为准。凡有脚本的检查项，报告里要贴命令与退出码。
3. **降级必须留痕**：任何因环境/资源/时间做出的妥协（样本量不足、依赖替换、精度降低），
   当场写进 `结果/降级记录.md`，交付时汇总成论文中的"偏差与降级声明"。
   没有降级记录的妥协视为隐瞒。

## 算法红线速览

判定标准以 references/algorithm-redlines.md 为准，通过标准以本文件各 Gate 为准，两处冲突时**取更严者**。红线是终止条件不是披露条件——发现违反必须修复，如实披露不构成豁免。

| # | 红线 | 违反即 |
|---|---|---|
| R1 | 题面口径忠实（含跨问一致、禁私货约束、量级合理性） | fail，不可豁免 |
| R2 | 家族匹配证书 + 选择认证分离 + 证书达标 | fail，不可豁免 |
| R3 | 最优性主张分级（global/local/best-found），无据则降措辞 | 降级（删改结论） |
| R4 | 核心原语神谕独立单测先行；且测试必须覆盖**四层**（原语／统计／**论文源**／**图**），见 computation-standards.md | fail，不可豁免 |
| R5 | 分辨率预算对齐 Δ（u ≤ Δ/3，Δ 来源受限） | 降级（降精度报告） |
| R6 | 数字单向流 + 三向审计 + stale 传播 + 复现可达 | fail，不可豁免 |
| R7 | 赛制合规（含页数口径：一切页数默认指 PDF 总页数，赛制另有明文时从其规定） | 阻断提交 |
| R8 | 每问必须有可见的算法段落（输入/步骤/输出/复杂度，能据以重写） | 降级（补写才算完成） |
| R9 | 每问写成形式化模型（决策变量／目标或判定准则／约束三要素齐），阈值反解类同样适用 | 降级（补写才算完成） |
| R10 | 由数据反推的结论必须附排他性检验（该观察在 not-X 下是否同样成立） | 降级（无排他性即降为假设） |
| R11 | 临界／最优／边界方案必须由**独立第二实现**复核，非同代码换种子 | fail，不可豁免 |
| R12 | 折回／镜像碎片不得跨电极（跨边界条件）短接；节点/边/电极判定同一身份口径 | fail，不可豁免 |
| R13 | **退化规模检验**：个体数=1／=0、阈值→0／→大，四行结果必须落盘且符合物理期望 | fail，不可豁免 |
| R14 | 最终答案与**题面给定档位**同量纲比较，差 ≥1 个数量级即回查口径 | fail，不可豁免 |
| R15 | 多档结果必须可分辨；全档相同（尤其全饱和/全零）＝口径错误，**不得当作样本量不足扩容** | fail，不可豁免 |

## 引用文件（强制读点）

| 文件 | 强制读点 |
|---|---|
| references/computation-standards.md | 进 P3 前 |
| references/algorithm-redlines.md | 进 P4 前（P2 证书预告时先查 R2 映射表） |
| references/depth-review.md | 进 P4 前 |
| references/figures-standards.md | 进 P5 前 |
| references/paper-latex.md | 进 P6 前 |
| references/compliance.md | P0 摘录条款时 + 进 P7 前 |

references 之间可交叉引用（红线↔规范↔审查互相指），按指针跳读即可；引用链之外不依赖任何本目录以外的资料。

**辅助脚本（`scripts/`，纯标准库，直接调用，别现写公式）**：

> 调用时一律带 `PYTHONDONTWRITEBYTECODE=1`（或跑完清 `__pycache__`）——否则 skill 目录会被自己的运行产物污染，违反临时文件纪律。这条是自检里真实抓到过的。


| 脚本 | 用途 | 典型调用 |
|---|---|---|
| certify.py | Wilson/CP **上下界** + **三态证书**（feasible／excluded／**inconclusive**）、Bonferroni 校正、由阈值反解所需样本量。区间跨过阈值时只能称「证据不足」，**不得称「不可行」**；inconclusive 会直接给出要下结论还需多少样本 | `python scripts/certify.py --k 39668 --n 40000 --threshold 0.90 --delta 0.005`；`--solve-n --p-hat 0.905 --threshold 0.90` |
| ledger.py | 账本结构校验（含唯一权威答案）、依赖哈希冻结、stale 传播、numbers.tex 宏生成 | `--validate` / `--freeze` / `--stale-check --write` / `--emit-tex -o 论文/numbers.tex` |
| audit_numbers.py | 论文↔账本↔结果文件三向审计 + 未走宏数字清单 | `--ledger … --numbers … --tex 论文/main.tex --out 结果/审计报告.md` |
| figqa.py | 空图/重复图/分辨率/未引用检查 + contact sheet | `python scripts/figqa.py 图/ --tex 论文/main.tex` |
| latex_gate.py | 编译日志阻断项/非阻断项分类、页数与摘要页核实 | `python scripts/latex_gate.py 论文/main.log --aux 论文/main.aux` |

`assets/reproduce.py`：**交付包复现入口模板**——P7 要求交付目录内必须有可执行入口（只读复核台账↔宏↔论文一致性 + 最小重算子集），不能只在 README 里写命令。

`assets/paper/`：**分节论文模板包**——主控 `main.tex` 装配 13 个分节 tex（摘要/重述/分析/假设/符号/四问/检验/评价/文献/附录），每个分节文件顶部写死该节的写作合同与常见扣分点；含 numbers.tex 注入位（带 `[NUMBERS-MISSING]` 显式失败标记）、编译修错循环速查表（README）。本机 xelatex 冒烟通过（5 页，退出码 0）。
分节而非单文件的理由：基线 A 与 Mrite 的 tex 链路都是分节 + 主控编译——编译错误可定位到节、出数快的问先写先编、改一节不重排全文、某问写长了可再拆「分析与准备 / 建模与求解」两片。**有官方模板时不得用它覆盖官方 class**，只沿用其分节装配结构。

## 加分项（门禁全绿且预算有余时）

从悬案赏金表取赏金/成本比最高的 1–2 项深挖，作为论文的增量发现（理论量级校验、判据裕度分析、数据反推的隐含结构都属此类）；预算不足时明确放弃并记录。

## 来源与许可

本 skill 的做法萃取自以下开源项目的实测赢家维度，**全部改写重实现，未复制上游文本**（逐项落位见 ATTRIBUTION.md）：sweetcornna/mathodology、zhnnky329/MathModeling-skills、Lupynow/math-modeling-skills（三者均有 MIT LICENSE 文件）；Rzna-5559/Mrite 仅 README 声明 MIT 而无授权正文，按"仅参考做法"处理并留证。无许可证仓库仅参考设计思想。深度审查层（五问五工件 + M1–M7）、红线体系与 `scripts/` 为原创，见 ATTRIBUTION 原创部分。
