# 开题参数（每场比赛开头填一次）

赛制数字不写死在 skill 里——它们随赛事变，硬编码就是过拟合。
门禁脚本从本文件取参数；**没填就报错，不给兜底默认值**（`scripts/openconf.py`）。

填之前先读赛事的论文格式规范原文，逐条抄进来，不要凭印象。

```yaml
赛事: 高教社杯全国大学生数学建模竞赛
正文页数上限: 20          # → latex_gate.py --max-body-pages（超出按赛制扣分）
总页数下限: 40            # → latex_gate.py --min-pages（不够就补附录，不要撑正文）
图总数下限: 12            # → figqa.py --min-figures
正文引用图下限: 8         # → figqa.py --min-body-figures
摘要页数: 1               # → latex_gate.py --abstract-label 程序化核验
论文模板: 官方 class 优先；无官方模板时用 assets/paper/format.cls
目标图样例目录: <填你自己的期刊图样例目录绝对路径>
总时限: 72h
主计算机时上限: 20h
本机核数: 32
```

## 两个数字为什么这么定

**页数下限不是"能交差"的档，是高分档。** 实测有一轮交付物精确停在当时写的下限上
（25 页、每问 1 张图），逐条合规却在图表维直接 0 分——**地板就是模型会停的地方**。
对照同题人工基线：总 53 页，正文受控、附录厚重。所以下限按高分基线定，并由退出码强制。

**总页数不够时补附录，不要撑正文。** 赛制通常只限正文；附录不限页，是承载工作量的地方
（结果明细表、补充图表、源程序、推导全过程、复现说明）。注水比短更糟，同时丢 paper 与 rules。

## 目标图样例目录

指向一个装着你认可的期刊级图的目录。画图前打开 2–3 张同图式的样例看一眼再动手，
模仿的是版面、色彩编码、轴的处理与图例组织——**长相可以仿，数字一个都不许仿**。
细则见 `references/figure-style.md`。
