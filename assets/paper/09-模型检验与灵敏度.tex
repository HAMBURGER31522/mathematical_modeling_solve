% ============================================================================
% 模型检验与灵敏度分析 —— 独立成节，不要塞进各问里草草带过。
% 合同：
%   1. 双路互证：同一结论用两条独立路径算（增量 vs 直算、解析 vs 仿真），
%      给出一致性证据。这一节最容易抓到自己的 bug——loopmw 的 -1 哨兵污染
%      就是被直算臂逮出来的（每档虚高一个失败率）。
%   2. 灵敏度：参数扰动 -> 结论是否翻转，给数字不给形容词。
%   3. 边界与失效域：模型在什么条件下不成立，如实写。
% ============================================================================
\section{模型的检验与灵敏度分析}

\subsection{交叉验证（双路互证）}
\subsection{参数灵敏度}
\subsection{适用边界与失效域}
