% ============================================================================
% 源程序 —— 核心代码清单（关键函数，不是全量粘贴）。
% 合同：贴的每段代码都要在正文/附录里被引用到；用 listings 保持可读；
% 与交付包 code/ 目录中的文件一一对应，禁止贴一份跑不通的版本。
% 篇幅参考：4-8 页。
% ============================================================================
\subsection{核心源程序}

\begin{lstlisting}[language=Python, basicstyle=\ttfamily\small, breaklines=true]
# 关键函数清单
\end{lstlisting}
