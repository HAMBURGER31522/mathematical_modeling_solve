% ── 全文通用形态（Mrite 高教社杯规范）──
% 正文一律自然段；禁分点符号、禁 \textbf（摘要的语义加重属于本节既定例外）。
% 本节合同显式要求的结构（如 itemize、\textbf 或分点）属于模板既定例外，以本节合同为准。
% 跨页表用 longtable，短表用 tabularx；列宽比例总和 = 1.04 − 0.04 × N（N 为列数），格内文字不换行。
% 图宽 0.8\textwidth，表宽 \textwidth；图题交给 \caption，绘图代码不写 set_title()；图表必须在正文被引用。
% 章与章、节与节之间一律不加 \newpage。
% ── 本节合同 ──
% 总字数 ≤900，开头段 ≤30 字，结尾段 ≤20 字；编译后必须恰为 1 页（\label{abstract:end} 供 .aux 程序化核验，不得删）。
% 每问一段，段内五动作固定：段首定位 → 建模主张 → 方法链条 → 结果句 → 验证句。
% 粗体只标出最小必要的模型、方法、决策、结果或短验证证据；无固定数量。
% 背景、题面复述、段首定位、连接词、常规处理、软件名和泛泛赞语保持正常字重。
% 写摘要前读 references/abstract-moves.md；加粗决策读 references/abstract-emphasis.md。
%
\begin{abstract}

% ==== 开头段（2句话）====
...是...领域的重要研究课题。本文基于...数据，围绕...展开系统研究。

% ==== 逐问摘要 ====
对于问题一，首先对...进行了...分析，接着采用...方法构建了...模型，最后通过...得到...。结果表明，...（关键数值）。

对于问题二，首先...，接着...，最后...。结果表明...。

对于问题三，首先...，接着...，最后...。结果表明...。

对于问题四，首先...，接着...，最后...。结果表明...。

% 仅在确有必要时，给可回指证据的短语加 \textbf{粗体}；不要给整句、段首或过程词加粗。

% ==== 结尾段（2句话）====
本文完成了...的研究，并如实说明模型的适用边界与证据强度。研究成果可为...提供决策参考。


\keywords{关键词1；关键词2；关键词3；关键词4；关键词5}

\label{abstract:end}
\end{abstract}
