% ============================================================================
% 摘要 —— 评委唯一必读页。
%
% 【真实评委反馈直接催生的规则，2026-09-04】
% 同题一份 53 页交付物拿到的评审意见是：
%   「摘要结构完整，能准确概括四问的模型、关键数值和主要结论，信息组织较强；
%     但**部分技术细节略密**，可进一步**突出核心创新和最终成果**。」
% 也就是说：结构对了，密度错了。摘要不是方法论的浓缩版，是「用了什么算法 + 得到什么结果」。
%
% 【摘要只回答两件事，每问一句】
%   1. 用了什么算法（算法名要加粗）
%   2. 得到什么结果（结论与关键数字要加粗）
% 技术细节（参数怎么调、边界怎么处理、为什么不用另一种口径）**一律留给正文**。
% 【合同先行】运行时审计链已完成逐问 JSON + checks 字段 + 聚合校验后，才从宏回填摘要。
%
% 【加粗规则（硬）】评委扫读时眼睛只会停在粗体上，该粗的没粗＝白写：
%   - 算法/模型名：\textbf{并查集}、\textbf{Wilson 置信区间}、\textbf{Logistic 反解}
%   - 每问的最终结论与关键数字：\textbf{组1 不导通，组2、组3 导通}、\textbf{0.87\%}、\textbf{9.13 元}
%   - 不要整句加粗——粗体太多等于没有粗体。一句里粗体不超过两处。
%
% 【逐条自查，缺一即未完成】
%   1. 每问一句，四问就四句，句式统一（\textbf{针对问题N：}开头）。
%   2. 每句 = 问题类型定性 + \textbf{算法名} + \textbf{结论/数字}。
%   3. 数字全部走宏，禁止手抄；机会约束类结论旁必须带置信下界（用 \xxxCert 宏一并带出）。
%   4. 摘要里每个数字必须属于台账中证书达标的权威条目。
%   5. 删掉任何一句若不损失「算法或结果」的信息，就删掉它。
%   6. 末句一句话点出模型特色/核心创新——这是评委找「创新点」的地方。
%   7. 赛制要求一页时用 scripts/latex_gate.py --abstract-label 从 .aux 程序化核实。
%
% 【禁区】套话开头（随着某某的发展）；创新点自夸列表；无数字的空洞形容；
%   把正文的推导细节搬进摘要（这是最常见的「技术细节略密」）。
% 末句的核心创新要回答评委看见什么证据，而不是重复方法名。
% ============================================================================
\begin{abstract}
% 第一句：问题背景 + 全文总体路线（一句话，点明方法族）。

\textbf{针对问题一：}本问是……问题。采用\textbf{（算法名）}，……。结果显示，\textbf{（结论 + 关键数字）}。

\textbf{针对问题二：}本问是……问题。采用\textbf{（算法名）}，……。结果显示，\textbf{（结论 + 关键数字）}。

\textbf{针对问题三：}本问是……问题。建立\textbf{（优化模型名）}并采用\textbf{（求解算法）}，……。
结果显示，\textbf{（结论 + 关键数字）}，置信下界 ……。

\textbf{针对问题四：}本问是……问题。建立\textbf{（优化模型名）}，以……为决策变量、……为目标、……为约束，
采用\textbf{（求解算法）}求解。结果显示，\textbf{（最优方案 + 目标值）}，……。

% 末句：模型特色 / 核心创新（一句，讲机理不讲形容词）。

\textbf{关键词：} 关键词一；关键词二；关键词三；关键词四
\end{abstract}
