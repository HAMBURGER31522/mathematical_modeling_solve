% ============================================================================
% 摘要 —— 评委唯一必读页，写作优先级最高的一页。
%
% 写作合同（缺一条即视为未完成）：
%   1. 逐问一句：方法 + 关键数值结论。四问就是四句，不多不少。
%   2. 每个数字都走宏（如 \qOneThreshold），禁止手抄——手抄必然与正文不一致。
%   3. 摘要里出现的每个数字必须属于台账中「证书达标」的权威条目。
%   4. 机会约束 / 概率类结论：点估计旁必须同时给出置信下界，只给点估计=过度主张。
%   5. 末句总括模型特色（一句，讲机理不讲形容词）。
%   6. 赛制要求一页时用 scripts/latex_gate.py --abstract-label 从 .aux 程序化核实，
%      不靠目测。
%
% 禁区：套话开头（随着某某的发展）、创新点自夸列表、无数字的空洞形容。
% ============================================================================
\begin{abstract}
% 第一句：问题背景与本文总体路线（一句话，讲清用了什么方法族）。

% 问题一：方法 + 结论（数字走宏）。
% 问题二：方法 + 结论。
% 问题三：方法 + 结论 + 置信下界。
% 问题四：方法 + 最优方案 + 目标值 + 可行性证据。

% 末句：模型特色（机理层面的一句话）。

\textbf{关键词：} 关键词一；关键词二；关键词三；关键词四
\end{abstract}
