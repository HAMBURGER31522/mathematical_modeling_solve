% ── 全文通用形态（Mrite 高教社杯规范）──
% 本节合同：第2章直接逐问写问题分析，最后放文章总体思路图，不设置章首总述。
% 当前四问骨架仅作示例；使用时必须按题面实际问题数 n 增删分析小节与总体图节点。
% 问题分析只解释题型、变量关系和方法路线，不写模型结果；各问流程图保留在第5章对应问题标题下。
% 流程图统一用 main.tex 中的 TikZ flowbox/arrow 样式；本章末总体图是全篇路线占位。

\section{问题分析}

\subsection{问题一的分析}

针对问题一，题目要求……，属于……类问题。本问拟采用……方法，量化……与……之间的关系，并为后续问题提供可复用的输入。

首先，对原始数据和题面条件进行核对；接着完成必要的数据清洗、变量转换和描述性分析；然后根据问题类型比较候选模型并确定主路线；最后明确模型输出、检验指标和与后续问题的数据接口。

\subsection{问题二的分析}

针对问题二，题目要求……，属于……类问题。本问在问题一的分析或结果基础上，进一步研究……，目标是……。

首先继承问题一中已经验证的数据口径和共享判定内核；接着构造问题二所需的决策变量、目标函数或判定准则；然后比较候选算法并说明选择依据；最后给出可复现的求解步骤、输出结构和验证安排。

\subsection{问题三的分析}

针对问题三，题目要求……，属于……类问题。本问重点处理……带来的影响，并与前两问保持变量、单位和边界口径一致。

首先说明问题三相对前两问新增的条件；接着确定新增数据、约束和误差来源；然后在共享内核上扩展模型或优化过程；最后说明独立核验路线、稳健性检查以及结果将如何服务于题目目标。

\subsection{问题四的分析}

针对问题四，题目要求……，属于……类问题。本问需要综合……指标进行……判定或评价。

首先定义对象身份、正负类和判定边界；接着完成特征整理、质量控制和必要的类别处理；然后建立主分类或评价模型并设计独立核验；最后说明阈值、证书、适用边界和论文中的降级表述。

针对以上问题，本文采用由数据理解、问题分解、模型建立、求解验证和结论输出组成的整体路线。文章总体思路如图\ref{fig:overall_flow}所示。

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=0.65cm]
    \node[flowbox] (input) {题目与数据};
    \node[flowbox, below=of input] (p1) {问题一：关系建模};
    \node[flowbox, below=of p1] (p2) {问题二：决策优化};
    \node[flowbox, below=of p2] (p3) {问题三：误差与扩展};
    \node[flowbox, below=of p3] (p4) {问题四：综合判定};
    \node[flowbox, below=of p4] (out) {检验、评价与结论};
    \draw[arrow] (input) -- (p1);
    \draw[arrow] (p1) -- (p2);
    \draw[arrow] (p2) -- (p3);
    \draw[arrow] (p3) -- (p4);
    \draw[arrow] (p4) -- (out);
  \end{tikzpicture}
  \caption{文章总体思路图}
  \label{fig:overall_flow}
\end{figure}
