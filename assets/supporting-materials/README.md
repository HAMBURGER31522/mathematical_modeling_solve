# 支撑材料模板

`AI工具使用详情.tex` 是独立 PDF 模板，不应并入论文附录。实际使用 AI 时，将它复制到最终支撑材料目录，填写真实记录并编译为 `AI工具使用详情.pdf`；未使用 AI 时不提交该文件。

论文附录只做文件索引、运行环境、复现入口和核心代码节选。完整可运行源码放在 `支撑材料/source/`，并在 `delivery-manifest.json` 标出入口。详情见 `references/competition-delivery.md`。
